# Style Anagram Engine

A streamlit-based application that generates stylized anagrams using Markov language models.

## Features

- Full strict anagram generation
- Style-based ranking (medieval / fantasy / modern / scifi)
- Markov probability scoring
- Web UI via Streamlit

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py